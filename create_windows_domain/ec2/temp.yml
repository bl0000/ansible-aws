---
- name: Create Linux EC2 VM and Keypair
  hosts: localhost
  gather_facts: no

  vars:
    vpc_name: "windows_domain"
    region: "eu-west-2"
    ec2_key_directory: "./"

  tasks:
    - name: Fetch VPC information
      amazon.aws.ec2_vpc_net_info:
        region: "{{ region }}"
      register: vpcs_info

    - name: Get VPC ID for "windows_domain"
      set_fact:
        vpc_id: "{{ item.vpc_id }}"
      loop: "{{ vpcs_info.vpcs }}"
      when: (item.tags.Name | default('No Name')) == 'windows_domain'

    - name: Fetch SG information
      amazon.aws.ec2_security_group_info:
        filters:
          vpc_id: "{{ vpc_id }}"
      register: sg_info

    - name: temp1
      debug:
        var: sg_info

    - name: Get SG ID for Allow SSH
      set_fact:
        sg_id: "{{ item.group_id }}"
      loop: "{{ sg_info.security_groups }}"
      when: item.group_name == "Allow SSH"

    - name: temp2
      debug:
        var: sg_id
