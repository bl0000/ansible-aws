---
- name: Create keypair
  hosts: localhost
  gather_facts: no
  
  vars:
    region: "eu-west-2"
    keypair_name: "windows_domain_keypair"

  tasks:
    - name: Delete EC2 keypair
      amazon.aws.ec2_key:
        name: "{{ keypair_name }}"
        region: "{{ region }}"
        state: absent
